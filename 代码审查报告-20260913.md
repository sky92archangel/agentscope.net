# AgentScope.NET 代码审查报告（P0/P1 实施后）

**审查日期**: 2026-09-13
**审查范围**: 本轮 P0（10 项）+ P1（15 项）实施的 114 个变更文件 + 新增 10 个项目
**审查方式**: 5 个并行深度审查（Core Agent 层 / Model·Formatter 层 / Harness / 扩展模块 / 协议层）+ 全解构建 + 全量测试

**总体验证结果**: 全部 src 项目与测试项目 **0 编译错误**；单元测试 **655/656 通过**（1 个预存失败，与本次改动无关）。

---

## 一、审查总结

并行实施暴露了三类系统性问题：

1. **假覆盖（最危险）**：部分"已完成"的功能只有实现没有消费闭环——调用链断裂，功能实际不可用（详见第四节死代码清单）
2. **接口变更未同步**：`IModel`/`ITool` 新增接口成员导致下游 Mock/扩展编译失败（已修复）
3. **安全检查形同虚设**：路径前缀检查、shell 只读判断存在绕过方式

---

## 二、本次已修复（10 项）

| # | 问题 | 修复 |
|---|------|------|
| 1 | 测试 Mock 7 处未实现 `IModel.SupportsNativeStructuredOutput[WithTools]` → 编译失败 | 补齐接口实现（AgentTests 等 5 文件） |
| 2 | `HigressRemoteTool` 未实现 `ITool.IsExternal` → 编译失败 | 补齐 |
| 3 | DI `ServiceCollectionExtensions.cs:83`：配置的默认模型被硬编码 `gpt-4o` 无条件覆盖 | `model = ...` → `model ??= ...` |
| 4 | MCP `SseMcpClient`/`StreamableHttpMcpClient` 初始化时 JSON-RPC **error 响应被误判为成功** | 检查 `result` 存在且 `error` 不存在，失败抛异常 |
| 5 | SSE 传输工具 schema 反序列化缺 `PropertyNameCaseInsensitive` → camelCase 字段全部解析为空 | 补齐序列化选项 |
| 6 | MongoDB `DisposeAsync` 永不释放（MongoClient 2.x 只实现 `IDisposable`）+ 非幂等 | 回落同步 Dispose + `_disposed` 防重入 |
| 7 | `DeliverArtifactTool` 路径穿越检查可被同前缀兄弟目录绕过（`..\out2\evil.txt`） | 校验目录分隔符边界 |
| 8 | GLM baseUrl `/api/paas/v4` + endpoint `/v1/chat/completions` 拼接出 404 | `OpenAIClient.BuildUrl` 泛化为匹配任意 `/vN` 版本段 |
| 9 | **`FinalAnswerFilterMiddleware` 全量缓冲破坏流式 + 白名单吞掉 `SummaryFinish(IsLast)`/`Error` 事件**（下游永远收不到最终答案） | 重写为流式透传版：仅延迟推理文本块，工具调用出现时丢弃中间文本；Summary/Error/IsLast 实时透传 |
| 10 | `SUMMARY_FAILED` 标记写在被丢弃的临时 Msg 上，永远无法外发 | 标记随 `CreateFinalResponse` 生成的最终消息外发 |

---

## 三、未修复的严重 bug（按优先级，建议下轮排期）

### 🔴 P0 级（功能不可用/安全）

**B1. Anthropic 非流式链路整体不可用（存量 bug）**
`Formatter/Anthropic/Dto/AnthropicContentBlock.cs:24` — 多态判别符 `block_type` 与 API 实际字段 `type` 不匹配：请求多出未知字段被 API 拒绝，响应解析直接抛异常且被裸 `catch` 吞成 null。需改为 `TypeDiscriminatorPropertyName = "type"` 并配套序列化注解。

**B2. Anthropic 流式链路双重错误（存量 bug）**
`Model/Anthropic/AnthropicModel.cs:198-256` — ① `options.Stream=true` 从不写入 `AnthropicRequest.Stream`，服务端不返回 SSE；② 用 `AnthropicResponse`（全 required 字段）反序列化 SSE 事件必抛异常。现成的 `AnthropicResponseParser.ParseStreamEvent` 从未被调用。

**B3. 四个提供商 Formatter 是死代码（P0-4 实施缺陷）**
`OpenAIModel` 的 formatter 注入点类型是封闭类 `OpenAIChatFormatter`，而 `GlmFormatter` 等继承 `OpenAIBaseFormatter`，类型不兼容；且 `GlmModel/KimiModel/MiniMaxModel/DeepSeekModel` 构造时根本不传 formatter。GLM 的参数钳制、Kimi 的采样固定、DeepSeek 的推理内容修复**全部不生效**。需将注入点改为 `OpenAIBaseFormatter` 并在构造时传入。

**B4. AsyncToolMiddleware "假 offload"（P0-5 实施缺陷）**
`Harness/Middleware/AsyncToolMiddleware.cs:81-174` — `await next()` 后工具已同步执行完，后台任务不调用任何工具，直接 `Complete(占位元数据JSON)`；所有工具（默认阈值 15s>0）无条件被"offload"。Agent 经 `wait_async_results` 拿到的永远是假成功元数据。

**B5. PermissionEngine 路径检查可绕过（安全问题）**
`Permission/PermissionEngine.cs:585-627` — ① 反向 StartsWith 允许参数路径是允许目录的**祖先**（传 `C:/` 直接通过）；② 无分隔符边界（`/home/user-evil` 匹配 `/home/user`）；③ `Contains(":")` 把 URL/时间戳误判为绝对路径；④ `IsReadOnlyShellCommand` 用 StartsWith：`ls; rm -rf /`、`cat x && curl ...`、`find -delete` 均被判只读放行。

**B6. GracefulShutdown 超时"强制中断"是空操作**
`Shutdown/GracefulShutdownMiddleware.cs:70-88` — 创建的 `CancellationTokenSource` 从未传入 `next(input)`（`AgentInput` 无 Token 字段），超时 Cancel 无人消费，请求实际不会被中断。另：超时请求不摘除注册表会每秒重复中断；并发请求的 finally 会误清其他请求刚标记的中断状态。

**B7. HITL 暂停-恢复没有闭环（P0-2 实施缺陷）**
`EnhancedReActAgent.cs:230-242` — `TryRecoverConfirmResultAsync` 恒返回 null；`AgentState.AskingAction` 无任何写入方（agent 只写私有字段 `_pendingAskingAction`）；`METADATA_CONFIRM_RESULTS` 全仓库不存在。跨调用恢复链路完全断裂，当前实为普通阻塞询问。

**B8. OTel 的 chat/execute_tool span 是死逻辑（P1-12 实施缺陷）**
`HarnessAgent.ExecuteWithMiddlewareAsync` 只调度 `OnAgentAsync`/`OnSystemPromptAsync`，`IHarnessMiddleware.OnToolExecutionAsync`/`OnModelCallAsync` **全工程无调度器调用**；`ctx.Items["tool_result:{id}"]`/`chat_usage`/`reply_id` 只有读取方没有写入方。实际只有 invoke_agent span 会产生。

**B9. McpServerRegistrar Stdio 连接三重缺陷（P0-5 实施缺陷）**
`Harness/Tools/McpServerRegistrar.cs:89-117` — ① 无连接超时（子进程不响应则永久阻塞）；② 初始化失败不 Kill 进程（每次失败泄漏一个僵尸进程）；③ stderr 重定向后从不消费（管道写满子进程死锁）。另：未发送 MCP 协议必需的 `notifications/initialized` 通知，多数真实 server 会拒绝。

**B10. WaitAsyncResultsTool/AsyncToolMiddleware 静态字典跨 session 串扰**
`WaitAsyncResultsTool.cs:32,35` — `PendingResults`/`EmptyPollCounters` 均为 static：多 session 互相污染熔断计数；`await next()` 抛异常时 TCS 永久滞留静态字典。timeout 无下限钳制（负数直接抛异常）；barrier 中某 token 取消会吞掉其余已完成结果。

### 🟠 P1 级（语义/一致性缺陷）

**B11. CompositeDistributedStore CAS 分布式语义失效** — 版本只存内存、从不读后端；多进程互不可见；"读-改-写"用法直接抛异常；`_ = SetAsync(...)` fire-and-forget 丢弃失败
**B12. MySqlDistributedStore `Dialect { get; init; }` 时序冲突** — 构造函数内 `EnsureTableAsync` 已用默认方言建表，初始化器后换方言导致建表与 CRUD 语法不一致
**B13. SqliteDialect TTL SQL 无效** — `datetime('now', '+@t seconds')` 参数在字符串字面量内不会被绑定
**B14. K8s `ResumeAsync` 丢弃恢复出的 PodName/PodIP，启动全新 Pod → 原 Pod 泄漏**
**B15. AgentRunMcpChannel WebSocket 缺陷** — 不检查 `EndOfMessage`（分片截断）、不按 JSON-RPC id 匹配响应、并发调用共享 `_ws` 抛 InvalidOperationException
**B16. `HarnessAgentBuilder` 对静态 `FileToolUtils.AllowedRoots` 无锁 read-modify-write + 把 workspace 写入全局静态使所有 Agent 可访问**
**B17. `DynamicSkillMiddleware` + `Toolkit.DeepCopy` 错绑** — DeepCopy 不克隆 aware 组件，同一 middleware 实例注册进多个副本互相覆盖 `_toolkit`
**B18. `StreamAsync(IEnumerable<Msg>)` 丢弃除最后一条外的全部消息；`StreamEventsAsync` 忽略 RuntimeContext（流式路径 AdditionalWorkingDirectory 检查整体失效）**
**B19. `ClearContext(userId, sessionId)` 忽略参数清空全部 memory**
**B20. A2A `IAgentRunner` taskId 缓存 ContainsKey+赋值竞态（应 TryAdd）；`AgentRequestOptions.SessionId/UserId` 被忽略**
**B21. Anthropic `MergeOptions` 只复制 7 个字段静默丢失 PromptCaching（应改用 `GenerateOptions.Merge`）；`ToolChoiceType.None` 映射为 Any 语义相反**
**B22. `AnthropicPromptCacheConfig.CacheTtl` 是无效配置（从未被读取）；`ApplyContentBlockCaching` 只认 assistant+TextBlock，与 Java"最后一条消息最后一块"语义不符**
**B23. `Msg.GetTextContent` 不识别 DataBlock 返回类型名字符串，波及 Anthropic/DashScope 多处**
**B24. AguiResumeCoordinator 无 TTL 过期 + 零调用方（字典无界增长）**
**B25. 模型上下文窗口 `GetWindowSize` 未实现最长前缀优先（`glm-5.2-air` 可能命中 `glm-5*`）；`DASHSCOPE` 与 `KnownWindows` 两表 `qwen-turbo` 数据不一致**

---

## 四、死代码 / 未接线清单（"看似已实现"的高危区）

以下功能**实现存在但消费闭环断裂**，接入前不会产生任何效果，反而给出虚假的完成度：

| 功能 | 断点 |
|---|---|
| AgUI 9 个新事件 record | 全工程无生产者/消费者/序列化路径；且无 JSON 协议注解（会序列化成数字枚举+PascalCase，不符合 AG-UI 规范） |
| A2A `artifact_type` metadata | 只有写入方（ReActAgentWithBuilderRunner），下游 executor 原样透传，无 Event→Artifact 转换 |
| A2A `TaskStore/QueueManager/PushSender` | 均为 `object?` 假注入点，无读取/转型代码 |
| `McpMeta` | 零引用 |
| `ToolContextState.SpawnRegistry/ReadCache` | 零调用方；且 `PendingAsyncToolIds` 用非线程安全 HashSet |
| `AgentState.ShutdownInterrupted` | 死属性，与 GracefulShutdownManager 内存字典脱节，进程重启无法恢复 |
| `MiddlewareChain.BuildReasoningChain/BuildActingChain/BuildSystemPromptChain` | 无调用方（三个拦截点未接线）；`ApplySystemPromptMiddlewareAsync` 传 `RuntimeContext.Empty` 也拿不到真实上下文 |
| `WaitAsyncResultsTool.ResetEmptyPollCounter` | 零调用方 |
| Anthropic `AnthropicChatFormatter.MergeOptions` | 死方法 |
| K8s `_connector` 字段 | 注入后从未使用（连接策略是摆设）；`PortForwardConnectorStrategy.Stop()` 零调用（进程泄漏）；端口固定 8888 二次转发必冲突 |
| E2B `ToMountArgs()` | 输出不含 AccessKey/SecretKey/ReadOnly（认证参数是死字段） |
| PostgreSql `SaveAsync/DeleteAsync/GetAllAsync` | 不在 `ISkillRepository` 接口中，经接口引用调用不到 |

---

## 五、结构性建议

1. **接口分裂治理**：仓库已存在 3 个同名 `IDistributedStore`（Core/State、Extensions/Store、Harness）与 2 个同名 `ISkillRepository`，DI 项目又自造 `IModelFactory`/`IToolFactory`。跨模块组合（如 Mongo store + DI）将需要大量适配，建议统一收敛到 Core。
2. **假覆盖验收标准**：后续移植应以"调用链闭环"为完成标准（生产者→消费者→序列化端到端可跑通），而非"类已创建"。
3. **预存失败测试**：`ReActAgent_WithToolAndMissingActionInput_ShouldNotReturnCompletionMarker`（AgentTests.cs:204）数据与断言自相矛盾（模型返回 `Action Input: Done` 却断言空文本），HEAD 即失败，应修正测试。
4. **Anthropic 存量问题优先级提升**：B1/B2（判别符+流式）意味着 .NET 版 Anthropic 从未真正可用过，建议作为独立 hotfix 处理。
5. **安全检查模式统一**：路径边界检查（B5/B7 已修 1/2）应抽成共享的 `PathSafety.IsUnderDirectory()` 工具方法，杜绝再次出现裸 StartsWith。

---

## 六、验证记录

| 项目 | 编译 | 测试 |
|---|---|---|
| AgentScope.Core | ✅ 0 err | — |
| AgentScope.Harness | ✅ 0 err | — |
| AgentScope.Tracing.OpenTelemetry | ✅ 0 err | — |
| AgentScope.Extensions.DependencyInjection | ✅ 0 err | — |
| AgentScope.Extensions.Store.MongoDB | ✅ 0 err | — |
| AgentScope.Extensions.Higress | ✅ 0 err | — |
| AgentScope.Core.Tests | ✅ 0 err | 655/656（1 个预存失败，见五.3） |
