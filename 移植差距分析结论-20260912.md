# AgentScope Java → .NET 移植差距分析结论

**分析日期**: 2026-09-12
**Java 基线**: `R:\_CODE_\JAVA\agentscope-java` main 分支 `2.0.3-SNAPSHOT`（最新提交 2026-09-12，5 月以来 85 个 feat 提交）
**.NET 基线**: v2.0.1（2026-08-17，develop/v2.0.1）

> 注：Java 本地仓库 tag 最新为 v2.0.3，pom 版本 `2.0.3-SNAPSHOT`。无论官方发布号是否为 2.0.8，本地 main 已包含全部最新功能，本分析以代码为准。

---

## 一、总体结论

.NET 版已完成"形"的移植（22 个核心模块 + 42 个扩展项目目录齐全），但**"神"差距明显**：Java 5-9 月的重构把主推 API 换成了洋葱中间件架构，并围绕 Harness 运行时、分布式快照、AG-UI 协议做了深度工程化。.NET 侧多处是**骨架级/空壳级**实现，典型对比：

| 组件 | Java | .NET |
|---|---|---|
| HarnessAgent | 2973 行 | 230 行 |
| HarnessGateway | 847 行 | 44 行 |
| AgentSpawnTool | 1795 行 | 74 行 |
| A2A AgentScopeAgentExecutor | 535 行 | 42 行 |
| AguiAgentAdapter（策略体系） | converter/enricher/processor 多层 | 146 行单文件 switch |
| OTel Tracing 中间件 | 345 行（usage 属性、span 完备） | 86 行（透传不追踪） |

**根因**：.NET 的 `MiddlewareBase`/`MiddlewareChain` 是孤儿代码——没有任何 Agent 接入中间件链，`EnhancedReActAgent` 仍是 HookManager 直调架构。这导致 GracefulShutdown、DynamicSkill、FinalAnswerFilter 等"中间件化功能"在 .NET 无法真正生效。

---

## 二、P0 高优先级缺口（建议立即排期）

### 1. 洋葱中间件架构接线（结构性，其他功能的前置）
- Java `ReActAgent`（5427 行）：四段拦截（onAgent/onReasoning/onModelCall/onActing），**每段返回完整事件流 `Flux<AgentEvent>`**，Builder 自动装配 GracefulShutdown/TaskReminder/DynamicSkill 中间件并按 `order()` 排序
- .NET：`Agent/MiddlewareBase.cs` + `MiddlewareChain.cs` 已定义但零引用；钩子只能改输入不能拦截事件流
- 这是移植 GracefulShutdown、FinalAnswerFilter、AsyncTool 等一揽子功能的地基

### 2. HITL 暂停-恢复完整链路
- Java：ASKING 状态持久化 → `RequestStopEvent(PERMISSION_ASKING)` 暂停 → `METADATA_CONFIRM_RESULTS` 跨调用恢复 → `UserConfirmResultEvent`（#2511）；`AllToolsDeniedEvent` 含纠偏提醒注入（#2083）
- .NET：只有同步 `ConfirmCallback`，事件 record 已定义但无发射点，无恢复机制

### 3. Anthropic Prompt Caching + CachedTokens（直接关系 token 成本）
- Java：`GenerateOptions.cacheControl/cacheTtl` + Anthropic/DashScope formatter 打 `cache_control: ephemeral`（5m/1h TTL，#2223/#2350）；`ChatUsage.cachedTokens`（#1868/#2568）
- .NET：⚠️ **存在移植错位**——`IFormatter.cs:227` 的 `GenerateOptions.CacheControl` 是响应缓存语义（`CachePolicy` 枚举），与 Java 同名不同义；`AnthropicTool.cs:70` 的 CacheControl record 零使用；ChatUsage 无 CachedTokens 字段

### 4. Model 提供商补齐（国内刚需）
| 提供商 | Java | .NET |
|---|---|---|
| GLM/Zhipu（#2316） | formatter + provider + 上下文表 | **完全没有** |
| Kimi/Moonshot（#2320） | formatter + provider | 仅 `KimiCredential.cs` 骨架（自述返回 null） |
| MiniMax（#2299） | 3 类 + 流式去重 | 无 |
| DeepSeek v4（#2307） | formatter + provider + 1M 上下文表 | 仅继承 OpenAIModel 的简单版 |
| ModelContextWindows | 按商分表，Qwen3/gpt-4.1/GLM/Kimi 全量 | 旧合并字典，缺全部新条目 |

### 5. Harness 运行时核心链路
- **MCP server 真实注册**：Java `McpServerRegistrar` 完整 stdio/sse/http 连接 + enableTools 过滤 + 结果上报 listener（#2877）；.NET 版是 24 行配置容器，`ToolsConfigLoader` 不解析 `mcpServers`
- **远程 subagent SSE 事件流 + HITL resume 闭环**（#2559/#2613/#2593/#2592）：.NET 协议类型齐但未集成
- **异步工具闭环**：Java AsyncToolMiddleware（超时 offload → 后台执行 → inbox 推送 + guardrails 熔断，#1802/#2093）；.NET 版是 54 行直通空壳
- **deliver_artifact 沙箱产物交付**（#2667）：完全缺失
- **web tools**：禁用开关（#3075）+ HttpClient 注入透传 subagent（#3103）+ 缺 `web_search`

### 6. AgUI 事件体系（差距最大的协议项）
- Java #2306：`AgentEventConverterRegistry` 13 个转换器 + enricher + RequestProcessor/AgentResolver/runtime 层；550 行 sealed 全量事件
- .NET：事件枚举齐全但 **record 只实现约一半**（缺 StateDelta/MessagesSnapshot/ActivitySnapshot/TextMessageChunk/ToolCallChunk/Raw/ReasoningEncryptedValue 等）；`RunAgentInput.Resume` 字段存在但**零消费逻辑**；无 `AguiResumeCoordinator`（#3100 permission resume 后保留 tool results）、无断连中断配置（#2719）

### 7. Permission 参数级求值
- Java `PermissionEngine`（335 行）：按工具名分表 + **参数级检查**（危险路径、EXPLORE/ACCEPT_EDITS 只读处理、bypass-immune）+ `AdditionalWorkingDirectory`
- .NET（295 行）：仅工具名通配符；AcceptEdits/Explore 模式定义了但 Evaluate 未实现；AdditionalWorkingDirectory 是死代码

### 8. GracefulShutdown 深度
- Java：超时监控线程（每秒 enforceTimeoutAndInterrupt 强断保存）、ActiveRequestContext、恢复标记、JVM 钩子
- .NET：126 行简化版，无超时强断、无接线（无洋葱链可挂）

### 9. MongoDB 扩展（#2698，2026-09-11 新增）
- Java 9 类（DistributedStore/BaseStore/AgentStateStore/SnapshotSpec/RemoteSnapshotClient/SandboxExecutionGuard/KeyEscaper/IndexUtils）
- .NET：无 MongoDB 项目

### 10. DI 集成包（对应 Java spring-boot-starters）
- Java 11 个 starter；.NET 全仓库唯一 DI 扩展是 `AddAgentScopeTracing()`，无 `Microsoft.Extensions.Hosting`/`IServiceCollection` 集成——对 ASP.NET Core 生态是刚需

---

## 三、P1 中优先级缺口

1. **DistributedStore 聚合架构**：Java 一站式装配（agentState/baseStore/snapshotSpec/executionGuard/messageBus/asyncToolRegistry/taskRepository/sessionTurnGate，385 行 + Builder）；.NET `IDistributedStore` 仅 4 个 KV 方法。各 store 扩展普遍缺 snapshot/guard 组件（Redis 13 类 vs 2 文件、Cos 缺 DistributedStore/SnapshotSpec、PgSQL 缺 snapshot）
2. **K8s 沙箱 agent-sandbox CRD 新架构**（#2308/#1789/#3025）：40+ 类（SandboxClaim/WarmPool CRD、4 种连接策略、warm pool 秒级获取、快照重绑）；.NET 92 行薄工厂自管 pod 旧模式
3. **A2A executor 对齐**：Java 535 行（delta→artifact 流式转换、阻塞/非阻塞双模式）；.NET 42 行。Server 无 TaskStore/transport SPI 注入点；client 缺 Thinking/Video/Hint/File/Data 块解析
4. **agent-protocol 服务端**：AgentFactory 路由（#2590）、TaskStore 与 WorkspaceManager 解耦（#2615）、RuntimeContextCustomizer（#2595）——.NET 只有客户端
5. **FinalAnswerFilterMiddleware**（#2926，流式体验关键）+ Core 层中间件 `order()`（#2532，Harness 层已有）
6. **MCP 增强**：`httpRequestCustomizer` per-request 动态 token（#1992，生产刚需）、protocolVersions（#1187）、McpMeta 注入（#1576）
7. **Skill 联动**：`DynamicSkillMiddleware` 缺 ToolkitAware/rebindToolkit（技能→工具组激活同步，#1828）；PostgreSqlSkillRepository 缺写入/删除/资源表；Nacos 缺枚举 API
8. **DataBlock 转换管线**（#1933）：`Message/DataBlock.cs` 类存在但 4 个 Formatter 的 MessageConverter 均无分支——消息模型有、管线未接
9. **原生 structured output with tools**（#1904）：.NET 仅提示词方式，IModel 无能力声明、ReActAgent 无支持
10. **Shutdown/State 细项**：`clearStateCache()`（#2572）、`clearContext(userId, sessionId)`（#2499）、ToolContextState 的 spawn registry + LRU 读缓存（#1817）、AgentState 内嵌子上下文（permission/tool/tasks/plan）、failed summary 终止状态保留（#2757）、`ReactConfig.stopOnReject`
11. **wait_async_results guardrails 补强**：缺 task_ids barrier/wait_all、120s 超时钳制、空等待熔断
12. **OTel 中间件补齐**：execute_tool span、`gen_ai.usage.*`（GenAiAttributes.cs 常量已定义未消费）、reply_id
13. **E2B/AgentRun 沙箱补强**：云桶挂载策略、snapshot refs、OSS/NAS 挂载、MCP channel
14. **jdbc 方言统一重构**（#2759）：.NET Store.MySql 无 dialect 抽象
15. **示例工程严重不足**：Java documentation/ 15 分类 vs .NET 5 个 demo；缺 codingagent/dataagent/CopilotKit（#2554）完整应用示例；缺 user binding preferences API（#2711）

---

## 四、暂缓项（不建议短期投入）

| 项 | 原因 |
|---|---|
| **agentscope-service 四平面平台**（#3080） | 独立产品级工程：209 Java + 505 Go（aistiod）+ 227 TSX（React Console）+ Helm/K8s 部署体系，人年级工作量。.NET 现有 Service/Discovery 可视为其发现机制最小子集，按需另立项 |
| Web Console transcript 大改版（#2640） | 依赖 service 平台存在 |
| docs 站 Mintlify 迁移（#3081） | 与移植无关 |
| Channel 每 渠道 OutboundClient/Registry | 现有 5 渠道可用，按需补 |

## 五、名不副实项（Java 主干也没有，无需追赶）

- **ContextCompressor 三策略压缩 / SummarySchema**：Java 仅依赖声明 + 注释预留，无实现
- **tree-sitter Bash AST**：仅 pom 依赖，无代码引用
- **Workspace 包**：Java 仅空壳 package-info
- **Edit/Glob/Grep 独立工具**：Java core 无（.NET 同样没有，将来可自主创新）

---

## 六、建议路线图

```
第一阶段（打地基，~2-3 周）
├─ 洋葱中间件接线：EnhancedReActAgent 迁移到 MiddlewareChain（含事件流拦截 + order()）
├─ 纠正 GenerateOptions.CacheControl 语义错位 → Anthropic prompt caching + ChatUsage.CachedTokens
└─ GLM/Kimi/MiniMax 提供商 + ModelContextWindows 数据表

第二阶段（补运行时，~3-4 周）
├─ HITL 暂停-恢复链路 + Permission 参数级求值 + GracefulShutdown 深度
├─ MCP server 真实注册 + httpRequestCustomizer
├─ AgUI 事件 record 补齐 + ResumeCoordinator + A2A executor 对齐
└─ MongoDB 扩展 + DI 集成包（AddAgentScope 扩展方法族）

第三阶段（追深度，按需）
├─ DistributedStore 聚合架构 + 各 store 的 snapshot/guard
├─ K8s agent-sandbox CRD + 远程 subagent SSE + 异步工具闭环
├─ DataBlock 管线 / structured output / FinalAnswerFilter
└─ 示例工程成体系补齐
```

**一句话结论**：.NET 版下一步的核心不是"补更多模块"，而是**把中间件架构接线、把骨架填成真实现**——优先级最高的是洋葱中间件接线（地基）、prompt caching + cachedTokens（成本）、GLM/Kimi 提供商（国内生态）、AgUI/A2A 协议深度（可用性）、Harness 运行时真实现（企业场景）。
